tenacity
ujson
Pillow
requests
ultralytics
supervision
transformers>=5.5.0
python-dotenv
google
google_genai
ollama
lmstudio
accelerate>=1.13.0
lxml
# vLLM, sglang, generic OpenAI-compatible backends
openai>=1.40.0
# Apple Silicon vision-language backend (MLXVLMComponent). Optional;
# installs gracefully as a no-op on Linux/Windows -- the backend just
# won't register.
mlx-vlm>=0.4.0 ; sys_platform == "darwin" and platform_machine == "arm64"
